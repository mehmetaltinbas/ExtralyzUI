import type { SectionBuilderStrategy } from '../section-builder-strategy.interface';

export const ProcessedSourcesPropsBuilderStrategy: SectionBuilderStrategy = {
    buildProps: async (tab) => {
        return {};
    },
};
